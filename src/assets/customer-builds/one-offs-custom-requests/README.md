# Prepared customer-build assets

Do not add camera originals directly here. Put originals in the matching category under `edit/customer-build-photos/`, then run `npm run photos:prepare`. The generated WebP files in this directory are the optimized site assets.
